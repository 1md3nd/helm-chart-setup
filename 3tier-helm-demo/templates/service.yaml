apiVersion: v1
kind: Service
metadata:
  name: {{ include "3tier-helm-demo.fullname" . }}
  labels:
    {{- include "3tier-helm-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "3tier-helm-demo.selectorLabels" . | nindent 4 }}
